## Layout rule
No UI elements may ever visually overlap, at any viewport width. Responsive layouts must collapse (icon-only buttons, hidden secondary content, horizontal scroll) rather than let flex/grid items compress into each other or spill text over neighbors.
